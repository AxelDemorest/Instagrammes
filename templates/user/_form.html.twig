{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn text-sm text-center bg-blue-300 hover:bg-blue-500 text-white py-2 px-3 rounded font-medium">{{ button_label|default('Sign Up') }}</button>
{{ form_end(form) }}
